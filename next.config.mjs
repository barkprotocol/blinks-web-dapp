/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: [
        'uploadcare.com',
        'another-example.com',
      ],
    },
  };
  
  export default nextConfig;